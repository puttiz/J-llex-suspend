!!! 5
%html
  %head
    %meta{charset: "utf-8"}
    %title {% if  page.title %}{{ page.title }} ~ {% endif %}{{ site:title }}
    %link{rel: "stylesheet", href: "/assets/c/style.css"}

  %body{id: "{{ post.category }}"}
    %article
      %a#home{href: "/"} Home
      %header
        %h1 {{ page.title }}
      .wrapper
        %time {{ page.date | date: '%B' }} {{ page.date | date: '%d' }} {{ page.date | date: '%Y' }}
        %section{role: "main"}
          %h2 {{ page.excerpt }}
          {{ content }}
    %footer
      %small © Puttiz
    %script{src: "/assets/j/site.js"}